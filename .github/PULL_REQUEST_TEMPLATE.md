# 变更说明

## Purpose
本次 PR 解决什么问题？

## Overview
- 改了哪些文件
- 为什么这么改

## Verification
- `pytest` 是否全绿
- `ruff check` 是否通过
- 是否重新生成过 README（`python3 -c "..."` 见 CI 日志）
- 截图 / 证据（如有 UI 改动）

## Reviewer guidance
- 是否需要特别关注某处
- 是否有意未处理的事项
